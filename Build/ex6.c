// find a perimeter of a rectangle
#include<stdio.h>
int main(){
float length, width, perimeter;
printf("enter length and width of a rectangle");
scanf("%f %f",&length, &width);
perimeter=2* (length+width);
printf("perimeter of rectangle =%f ",perimeter);
}
